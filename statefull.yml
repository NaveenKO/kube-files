
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: myde
spec:
  selector:
    matchLabels:
      app: paytm
  template:
    metadata:
      labels:
        app: paytm
    spec:
      containers:
        - name: con1
          image: shaikmustafa/dm
          ports:
            - containerPort: 80
